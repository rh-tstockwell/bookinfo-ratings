spec:
  template:
    spec:
      containers:
        - name: ratings
          env:
            - name: SERVICE_VERSION
              value: v2
            - name: MONGO_DB_URL
              value: mongodb:27017

